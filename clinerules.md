[## 禁止事项

- 不要自动生成MD文档文件，除非我要求你生成我符合我的需求的
- 不要复制粘贴大量代码
- 不要重复描述已有内容
- 不要在PowerShell中使用`&&`（用分号`;`）

## 常见错误规避

1. **PowerShell命令分隔**：使用`;`而不是`&&`
   - ❌ `cd dir && python script.py` (会失败)
   - ✅ `cd dir; python script.py` (正确)

2. **自动生成文档**：不要创建README.md等文档文件
   - 引用现有文件，不生成新文档

## 重要原则

1. 尽量节省token
2. 生成的代码均按照rag_demo_claude.py的风格，要求注释明确，逻辑清晰，适合新手阅读
3. 不要对rag_demo_claude.py做出重大结构修改，尽量符合对扩展开放，对修改关闭的理念]()